spring:
  application:
    name: frcunha-greeting-serice
    
# expor endpoints do actuator -> * tudo, poderia ser 'refresh' para atualizar o config server    
management:
  endpoints:
    web:
      exposure:
        include:
        - '*'
    
greeting-service:
  greeting: "Pronto Fred"
  default-value: "Mondo"    
    
